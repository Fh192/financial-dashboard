# Скопируйте в .env и заполните значениями из Vercel → Storage → Neon.

# Владелец БД: под ним dbmate применяет миграции
DATABASE_URL=

# Роль приложения с ограниченными правами: под ней работает Next.js
POSTGRES_URL=

# Секрет NextAuth: `npx auth secret` или `openssl rand -base64 32`
AUTH_SECRET=
