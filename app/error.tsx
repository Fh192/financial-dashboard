"use client";

import { ErrorState } from "@/components/error-state";

// Ошибки вне страниц панели, в том числе в layout панели
// (например, не удалось проверить сессию, потому что недоступна БД)
export default ErrorState;
